# 🔧 คู่มือแก้ไขปัญหา

## ปัญหา: Error เมื่อล็อกอิน "This page isn't working"

### สาเหตุที่เป็นไปได้:
1. **Migration ยังไม่ได้รัน** - ตาราง Category ยังไม่มีในฐานข้อมูล
2. **มี Transaction เก่าที่ไม่มี Category** - จากการเปลี่ยนแปลงโมเดล

### วิธีแก้ไข:

#### ขั้นตอนที่ 1: รัน Migrations
```bash
python manage.py migrate
```

#### ขั้นตอนที่ 2: สร้างหมวดหมู่เริ่มต้น
```bash
python manage.py create_categories
```

#### ขั้นตอนที่ 3: ตรวจสอบการตั้งค่า
```bash
python check_setup.py
```

#### ขั้นตอนที่ 4: รีสตาร์ทเซิร์ฟเวอร์
1. หยุดเซิร์ฟเวอร์ (กด Ctrl+C)
2. รันใหม่:
```bash
python manage.py runserver
```

### หากยังมีปัญหา:

#### ตรวจสอบว่า Migration รันสำเร็จหรือไม่:
```bash
python manage.py showmigrations ICANDEP
```

ควรเห็น:
```
ICANDEP
 [X] 0001_initial
 [X] 0002_transaction_user
 [X] 0003_alter_transaction_user
 [X] 0004_category_alter_transaction_category
```

#### หากมี Transaction เก่าที่ไม่มี Category:
1. เข้า Django Admin: `http://127.0.0.1:8000/admin/`
2. ไปที่ Transactions
3. แก้ไข Transaction ที่ไม่มี Category โดยเลือก Category ที่เหมาะสม

หรือใช้ Django Shell:
```bash
python manage.py shell
```

```python
from ICANDEP.models import Transaction, Category

# หา Transaction ที่ไม่มี Category
transactions_without_category = Transaction.objects.filter(category__isnull=True)

# แสดงจำนวน
print(f"พบ {transactions_without_category.count()} รายการที่ไม่มี Category")

# หากต้องการลบ (ระวัง!)
# transactions_without_category.delete()
```

### ตรวจสอบ Logs:
ดู error message ใน terminal ที่รัน `python manage.py runserver` เพื่อดูรายละเอียดข้อผิดพลาด

### ติดต่อ:
หากยังแก้ไขไม่ได้ กรุณาแจ้ง error message ที่เห็นใน terminal
